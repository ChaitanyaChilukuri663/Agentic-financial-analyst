# data/

Local scratch space for fetched filings, embeddings caches, and vector indexes.
Everything except this file is gitignored (see `.gitignore`) — nothing here is
committed. Populated by the ingestion pipeline (P3).
