"""LedgerLens — a grounded financial-QA engine.

The LLM proposes a *symbolic reasoning program*; a deterministic executor runs the
math. Every answer is retrieved from real filings, cited, and verified against
expert ground truth.
"""

__version__ = "0.1.0"
